A static build for imagemagic on alpine with musl.

Currently has enabled support for `jpeg`, `png`,`xml`,`lzma`,`zlib`. If you
require more features open an issue and I'll try to include it as well.
